{
  "name": "docker-strapi",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    // The whole repo is image builds and smoke tests, so a daemon is the
    // point. docker-in-docker keeps built test images out of the host's
    // image list.
    "ghcr.io/devcontainers/features/docker-in-docker:4": {
      "version": "latest",
      "dockerDashComposeVersion": "v2"
    },
    // python, pre-commit, shellcheck, actionlint and trivy are not features
    // any more: mise.toml pins them and post-create runs `mise install`.
    // One version list, shared with CI.
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "configureZshAsDefaultShell": true
    }
  },
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "timonwong.shellcheck",
        "foxundermoon.shell-format",
        "redhat.vscode-yaml"
      ],
      "settings": {
        "files.insertFinalNewline": true,
        "files.trimTrailingWhitespace": true,
        "files.eol": "\n",
        "[shellscript]": { "editor.tabSize": 2 },
        "yaml.format.enable": false
      }
    }
  }
}
